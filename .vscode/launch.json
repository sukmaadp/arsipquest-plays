{
    "version": "0.2.0",
    "configurations": [
        


        {
            "type": "chrome",
            "request": "launch",
            "name": "Jalankan via Live Server (Port 5500)",
            "url": "http://localhost:5500/index.html",
            "webRoot": "${workspaceFolder}"
        },
        {
            "type": "chrome",
            "request": "launch",
            "name": "Jalankan via Localhost (Port 8080)",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}